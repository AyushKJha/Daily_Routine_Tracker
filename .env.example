# Copy to .env and set locally. Never commit .env.
APIFY_API_TOKEN=
# Adapter uses this documented prompts/reply schema. No actor runs until both values exist.
APIFY_CHAT_ACTOR=zerobreak/chatgpt-prompt-runner
HOST=127.0.0.1
PORT=8000
COOKIE_SECURE=0
